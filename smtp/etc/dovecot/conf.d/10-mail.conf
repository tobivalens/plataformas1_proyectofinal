# ==== Ubicación del buzón de correo ====
# Usa formato Maildir dentro del home del usuario (~), compatible con Postfix.
mail_location = maildir:~/Maildir

# ==== Definición del namespace principal ====
# Define el buzón INBOX del usuario como bandeja de entrada principal.
namespace inbox {
  inbox = yes
}

# ==== Grupo privilegiado para acceso al correo ====
# Permite que Dovecot acceda a /var/mail y directorios de buzones si es necesario.
mail_privileged_group = mail