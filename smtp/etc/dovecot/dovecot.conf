# ==== Protocolos habilitados (IMAP, POP3 y LMTP para entrega local) ====
protocols = imap pop3 lmtp

# ==== Interfaces de red habilitadas (IPv4 e IPv6 en todas las IP) ====
listen = *, ::

# ==== Inclusión de configuraciones modulares de Dovecot ====
!include_try /usr/share/dovecot/protocols.d/*.protocol
!include conf.d/*.conf
!include_try local.conf

# ==== Diccionario interno (usado por plugins, opcional) ====
dict {
  # Ejemplo: quota = mysql:/etc/dovecot/dovecot-dict-sql.conf.ext
}